# XIUNO XW

基于 [Xiuno BBS 4.1.1](https://bbs.xiuno.com/) 二次开发的分支版本（fork），国产轻量论坛系统。

> 上游 Xiuno BBS 4.1.1 兼容 PHP 8.0 - 8.4，修复了 SMTP 邮件系统，升级了缓存机制（文件缓存 / Redis 认证）。XIUNO XW 在此基础上继续演进。

## 特性

- 轻量：仅 20 多个表，源码压缩后 1M 左右，单次请求处理在 0.01 秒级别
- 响应式：前端 BootStrap 4.5 + jQuery 3.5.1，自适应手机 / 平板 / PC
- 高性能：静态语言编程风格，充分发挥 PHP7+ OPCache 威力；Session + Token 双登录
- 插件机制：hook + overwrite，方便二次开发，不影响性能
- 多语言：自带简中 / 繁中 / 英文 / 俄语 / 泰语
- 强扩展：内置火车头免登录发布接口（loco.php）

## 更新日志

### v1.3.1 (2026-08-26)

- 修复后台手机端「版块管理」列表：表格列被挤压导致名称/排序输入框过窄无法点击，改为输入框最小宽度 + 左右横滚显示
- 修复后台手机端「版块编辑」页标签插件(xn_tag)标签名称/分类/排序输入框被挤压无法点击，标签表格支持横向滚动
- 后台 admin.css 新增手机端适配规则（<992px），主题装不装均生效

### v1.3.0 (2026-08-24)

- 新增内置插件「豆瓣影视批量导入」(xw_douban_import)：导入 TXT/XLSX/CSV 影视清单（每行：影视名|网盘链接），自动抓取豆瓣信息，生成与「大白·TinyMCE编辑器」完全一致的豆瓣卡片帖
- 网盘链接以 `[pd]` 短代码发布，由「网盘链接转二维码(pandown)」渲染为品牌色下载按钮 + 扫码二维码弹窗
- 同名影片自动合并：影视名相同但网盘链接不同时不再重复发帖，新链接自动追加到旧帖下载区（可切换为总是发新帖）
- 支持选择发布版块、勾选 xn_tag 固定标签、跳过重复条目；任务队列逐条发布带进度条，失败可改名重试，任务记录支持导出 CSV
- 新增文档 `docs/火车头采集发布.md`（火车头免登录发布接口 loco.php 使用说明）

### v1.2.9 (2026-08-24)

- pandown 网盘链接转二维码插件升级 v1.2.0：新增光鸭网盘（guangyapan.com）链接识别与支持
- pandown 插件新增后台管理图标

### v1.2.8 (2026-08-23)

- 修复轻鸿主题编辑器内豆瓣卡片样式丢失：主题的 `updateTinyMCETheme` 原用 `head.innerHTML` 整体覆盖编辑器 iframe 头部，导致 TinyMCE `content_css` 注入的样式被清除；改为追加 `<style>` 方式渲染
- 现在编辑器内卡片样式与前台一致（不再出现"手打样式"），切换明暗模式同样生效

### v1.2.7 (2026-08-23)

- 修复发行版安装环境检测失败：log/tmp/upload 空目录未包含在发行包导致"不可写"，现补占位文件随发行版分发，安装检测会自动创建缺失目录
- 安装检测增加目录自动创建逻辑（含 upload/tmp、upload/attach、upload/avatar、upload/forum 子目录）

### v1.2.6 (2026-08-23)

- 大白·TinyMCE 编辑器恢复原始豆瓣卡片样式：浅色卡片 + 左侧小海报 + 标题(可点击跳转) + 橙色评分条 + 元数据行 + 剧情简介 + 豆瓣热评 + 剧照区
- 豆瓣卡片改为占满内容区宽度（去除 max-width 限制，水平居中）

### v1.2.5 (2026-08-23)

- xn_tag 标签插件前台显示增强 v2.4：标签改为方框包裹风格，带 hover 上浮/点击按下/选中高亮交互
- 标签颜色完全适配主题 CSS 变量（`--primary`/`--secondary`/`--success`/`--danger`/`--warning`/`--info`/`--gray-*`），自动支持亮/暗色模式切换
- 版块页筛选栏改为 flex 布局，当前筛选标签自动高亮（主色背景）

### v1.2.4 (2026-08-22)

- 自动更新新增回滚/备份机制：每次更新/回滚前自动备份主程序到 `backup/`（仅保留最近 2 份），支持一键回滚到任意历史版本、删除旧备份
- 备份目录独立于 `tmp/`，不随清理缓存被删除；自动写入 `.htaccess` + `index.html` 防止 Web 直访
- 更新覆盖跳过 `conf` / `upload` / `log` / `tmp` / `backup`，发行版插件目录为空时跳过避免误删本地插件

### v1.2.3 (2026-08-22)

- 修复 tt_check 审核插件设置页复选框无法勾选：适配轻鸿主题的 `appearance:none` 样式，恢复原生复选框显示

### v1.2.2 (2026-08-22)

- 轻鸿主题新增「浏览器标签图标(favicon)」设置：后台 → 插件 → 轻鸿主题 → 设置 → 全局设置，可自定义标签页图标（默认回退 `view/img/favicon.ico`）

### v1.2.1 (2026-08-22)

- 修复 xn_mypost「我的回帖」：仅显示回帖，过滤掉主题帖（`isfirst=0`），版本升至 1.5
- 插件目录纳入仓库（38 个插件随发行版分发）

### v1.2.0 (2026-08-22)

- 后台「检测更新」新增一键自动更新：下载发行版附件（附件优先，源码包兜底）→ 自动备份 → 解压覆盖 → 清缓存，全自动完成
- 自动更新支持 zip / tar.gz 包，覆盖前自动备份 `conf` / `upload` / `log`，发行版含插件时同步更新插件目录
- xn_tag 标签选择列表显示位置调整：从版块介绍框上方移至版块介绍框下方、帖子列表上方（兼容默认主题与轻鸿主题）

### v1.1.1 (2026-08-21)

- 后台「基本设置」新增「在线保持时间」配置项（5 语言）
- 后台 footer / 按钮 / 导航配色与前台统一（Typecho 清爽风）
- 发帖页支持「阅读权限」（tt_read 插件）与「隐藏内容」「网盘链接」插入（兼容默认编辑器与 TinyMCE）
- 新增插件开发文档 [docs/插件开发指南.md](docs/插件开发指南.md)
- 插件目录改为单独分发（不再纳入仓库）

### v1.1.0 (2026-08-19)

- 全新 Typecho 清爽风原生主题：前台 / 后台 / 安装页统一白底导航、卡片圆角、清爽蓝主色
- 会话机制优化：游客纯浏览不再产生 session 记录，在线人数统计回归准确
- CSRF Token 改为 Cookie（HttpOnly）存储，避免页面渲染产生 session 数据
- 修复发帖成功后跳转版块异常（不再出现 `?forum-.htm`）
- 后台用户组统计图表修复（空数据处理、尺寸自适应）
- 每日签到插件 UI 适配新主题
- 移除轻鸿主题内置授权验证机制
- 多项 PHP 8 兼容与稳定性修复

## 与原版的差异

- 安全加固：loco 接口密码配置化；登录 Token 恢复 IP 绑定与过期校验；安装器移除已废弃的 `mysql_*` 代码；附件上传移除 exe/bin/swf 危险类型
- 存储引擎：全表 InnoDB
- 其他改动见 [UPGRADE.txt](UPGRADE.txt)

## 环境要求

- PHP >= 7.2（推荐 7.4 / 8.x）
- MySQL 5.7+（需要 pdo_mysql）
- 可选：Redis / Memcached（缓存加速）

## 安装

1. 上传所有文件到网站根目录，确保以下目录可写：`./upload`、`./plugin`、`./tmp`、`./log`、`./conf`
2. 浏览器访问 `http://www.domain.com/install/`，按提示完成安装
3. 安装完成后删除 `install` 目录

详细步骤见 [INSTALL.txt](INSTALL.txt)。

## 目录结构

```
├── admin/          后台
├── conf/           配置目录
├── index.inc.php   入口路由
├── lang/           语言包
├── model/          模型层（编译缓存到 tmp/）
├── plugin/         插件目录
├── route/          路由层
├── view/           模板与静态资源
├── xiunophp/       自研 PHP 框架（已压缩合并为 xiunophp.min.php）
└── install/        安装向导
```

## 二次开发

- 插件机制：`hook`（模板钩子）+ `overwrite`（类覆盖），参考 `plugin/` 下插件示例
- 编译缓存：源码改动后清空 `tmp/` 目录，或设置 `DEBUG=1` 开发模式
- 版本号在 `index.php` 中统一定义，避免手工修改 conf
- 插件开发指南：[docs/插件开发指南.md](docs/插件开发指南.md)

## 授权

MIT 协议。基于 Xiuno BBS 4.1.1（MIT）二次开发，保留了原作者的版权信息。

上游作者：axiuno@gmail.com · [bbs.xiuno.com](https://bbs.xiuno.com/)